services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: resonode
      POSTGRES_PASSWORD: resonode
      POSTGRES_DB: resonode
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U resonode"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://resonode:resonode@postgres:5432/resonode
      REDIS_URL: redis://redis:6379/0
      CORS_ORIGINS: '["http://localhost:3000"]'
      AUTH_MODE: development
      JWT_SECRET: dev-jwt-secret-change-in-production-min-32-chars
      WEB_APP_URL: http://localhost:3000
      WORKOS_REDIRECT_URI: http://localhost:8000/auth/callback
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: services/worker/Dockerfile
    environment:
      REDIS_URL: redis://redis:6379/0
      DATABASE_URL: postgresql+asyncpg://resonode:resonode@postgres:5432/resonode
      INFERENCE_BASE_URL: ""
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
