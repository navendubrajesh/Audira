{
  "name": "resonode",
  "private": true,
  "version": "0.0.0-phase0",
  "description": "Resonode — Enterprise Communications Neuro-Analyzer",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@resonode/web",
    "build:web": "npm run build --workspace=@resonode/web",
    "lint:web": "npm run lint --workspace=@resonode/web",
    "test:web": "npm run test --workspace=@resonode/web"
  },
  "engines": {
    "node": ">=20"
  }
}
