# TRIBE v2 GPU inference — install on GPU hosts only (Modal, RunPod, etc.)
# Not included in main API/worker requirements (no GPU on Render/Vercel).

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
httpx>=0.28.0

# Install TRIBE v2 from Meta — requires Python 3.10+, CUDA, and HF login for LLaMA 3.2
# pip install git+https://github.com/facebookresearch/tribev2.git
# Or follow: https://huggingface.co/facebook/tribev2
